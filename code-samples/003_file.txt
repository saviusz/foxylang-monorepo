func<OutputT, ErrorT> nazwa(int a, string b) {
    
    /*
    Zmienna z tekstem
    */
    final string elo = "elmo mordeczko";
    
    // Zmienna z liczbką
    const elmo53 = 89;
    
    string mamamio = nazwa('test', `różnych opcji`);

    return 0;
}